# Snake-in-Rust

rust interpretation of a sophomore year project that was previously done in c


```bash
cargo run -- 1
```

Arguments:
-   `1` or `0`: 1 = Snake grows on eat, 0 = Snake does not grow.






